use elura::world::{WorldModule, WorldModuleRegistry};
use elura::Result;

pub struct {{TYPE}}Module;

impl WorldModule for {{TYPE}}Module {
    fn name(&self) -> &str {
        "{{MODULE}}"
    }

    fn register(&self, world: &mut WorldModuleRegistry<'_>) -> Result<()> {
        // Declare generated route modules above and call each route's
        // mount function here, for example:
        // list_items::mount(world)?;
        let _ = world;
        Ok(())
    }
}
